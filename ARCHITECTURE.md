# SpendSmart AI — Architecture

## Overview

SpendSmart AI is a full-stack Next.js 14 application using the App Router. User submits a form → server runs audit logic → result stored in Supabase → UI renders from stored data.

---

## System Diagram